#!/bin/bash
# Claude Code PreToolUse hook — question-waiting indicator for AskUserQuestion

set -euo pipefail
trap 'exit 0' ERR

source "${BASH_SOURCE[0]%/*}/claude-window-lib"
cw_init
cw_rename_symbol "?"
